class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        ans = 0
        for i in range(1, n + 1):
            num = nums[i - 1]
            ans ^= num
            ans ^= i
        return ans      
