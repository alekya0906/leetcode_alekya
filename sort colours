class Solution:
    def sortColors(self, nums: List[int]) -> None:
        red, white = nums.count(0), nums.count(1)

        for i in range(len(nums)):
            if i < red:
                nums[i] = 0
            elif i < white + red:
                nums[i] = 1
            else:
                nums[i] = 2
